# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'count0604' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for count0604
pod ’SVProgressHUD’

  target 'count0604Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'count0604UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
